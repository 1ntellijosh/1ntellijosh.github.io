/**
 * Keyboard control enums - all keyboard control enums defined in one place
 * 
 * @since abstract--JP
 */

export enum KeyboardControlEnums {
  W = 'w',
  A = 'a',
  S = 's',
  D = 'd',
  SPACE = 'space',
}